---
import Layout from "../../layouts/Layout.astro";
import Header from "../../components/Header.astro";

// 工具接口定义
interface Tool {
  id: string;
  name: string;
  description: string;
  icon: string;
  url: string;
  category: string;
  features: string[];
  comingSoon?: boolean;
}

// 工具列表数据
const tools: Tool[] = [
  {
    id: "ip-info",
    name: "IP信息查看器",
    description: "查看访问者的外网IP地址、地理位置、ISP信息等详细网络信息",
    icon: "🌐",
    url: "/tools/ip-info",
    category: "网络工具",
    features: ["IP地址检测", "地理位置", "ISP信息", "网络类型"],
  },
  {
    id: "qrcode-generator",
    name: "二维码生成器",
    description:
      "在线生成二维码，支持文本、URL、自定义颜色、添加Logo和多格式下载",
    icon: "📱",
    url: "/tools/qrcode-generator",
    category: "网络工具",
    features: ["实时生成", "自定义样式", "添加Logo", "多格式导出"],
  },
  {
    id: "password-generator",
    name: "密码生成器",
    description: "生成安全的随机密码，支持自定义长度、字符类型和复杂度要求",
    icon: "🔐",
    url: "/tools/password-generator",
    category: "安全工具",
    features: ["自定义长度", "字符类型选择", "强度检测", "批量生成"],
  },
  {
    id: "url-encoder",
    name: "URL编解码工具",
    description: "URL编码和解码工具，支持中文URL编码、批量处理和格式验证",
    icon: "🔗",
    url: "/tools/url-encoder",
    category: "编码工具",
    features: ["URL编解码", "中文支持", "批量处理", "格式验证"],
  },
  {
    id: "base64-encoder",
    name: "BASE64编解码",
    description: "BASE64编码和解码工具，支持文本和文件的编解码操作",
    icon: "🔄",
    url: "/tools/base64-encoder",
    category: "编码工具",
    features: ["文本编解码", "文件支持", "批量处理", "格式验证"],
  },
  {
    id: "archive-extractor",
    name: "在线解压缩工具",
    description:
      "强大的在线解压缩工具，支持ZIP、RAR、7Z、TAR等多种格式，支持大文件处理",
    icon: "📦",
    url: "/tools/archive-extractor",
    category: "文件工具",
    features: ["多格式支持", "大文件处理", "预览功能", "选择性解压"],
  },
  {
    id: "image-compressor",
    name: "图片压缩工具",
    description:
      "智能图片压缩与格式转换，支持JPEG、PNG、WebP、AVIF等格式，保持高质量",
    icon: "🖼️",
    url: "/tools/image-compressor",
    category: "文件工具",
    features: ["智能压缩", "格式转换", "尺寸调整", "即时预览"],
  },
  {
    id: "json-formatter",
    name: "JSON格式化工具",
    description: "格式化、验证和美化JSON数据，支持语法高亮和错误检测",
    icon: "📋",
    url: "/tools/json-formatter",
    category: "开发工具",
    features: ["JSON格式化", "语法验证", "压缩/美化", "错误定位"],
  },
  {
    id: "cron-expression",
    name: "Cron表达式工具",
    description:
      "Cron表达式生成、解析和验证工具，支持多种格式和下次执行时间预测",
    icon: "⏰",
    url: "/tools/cron-expression",
    category: "开发工具",
    features: ["表达式生成", "语法验证", "时间预测", "可视化编辑"],
  },
];

// 按类别分组工具
const toolsByCategory: Record<string, Tool[]> = tools.reduce(
  (acc, tool) => {
    if (!acc[tool.category]) {
      acc[tool.category] = [];
    }
    acc[tool.category].push(tool);
    return acc;
  },
  {} as Record<string, Tool[]>
);
---

<Layout
  title="开发工具集合 - 数字花园"
  description="实用的开发工具集合，包含IP查看器、密码生成器等常用开发工具"
>
  <Header />

  <main class="main">
    <div class="container">
      <header class="page-header">
        <h1 class="page-title">开发工具集合</h1>
        <p class="page-description">
          精心收集的实用开发工具，提升你的开发效率和工作体验
        </p>
        <div class="tools-stats">
          <div class="stat">
            <span class="stat-number"
              >{tools.filter((t) => !t.comingSoon).length}</span
            >
            <span class="stat-label">可用工具</span>
          </div>
          <div class="stat-divider">|</div>
          <div class="stat">
            <span class="stat-number"
              >{tools.filter((t) => t.comingSoon).length}</span
            >
            <span class="stat-label">即将推出</span>
          </div>
          <div class="stat-divider">|</div>
          <div class="stat">
            <span class="stat-number"
              >{Object.keys(toolsByCategory).length}</span
            >
            <span class="stat-label">工具分类</span>
          </div>
        </div>
      </header>

      <!-- 工具分类展示 -->
      <section class="tools-section">
        {
          Object.entries(toolsByCategory).map(([category, categoryTools]) => (
            <div class="category-section">
              <h2 class="category-title">{category}</h2>
              <div class="tools-grid">
                {categoryTools.map((tool) => (
                  <article
                    class={`tool-card industrial-border ${tool.comingSoon ? "coming-soon" : ""}`}
                  >
                    <div class="tool-header">
                      <div class="tool-icon">{tool.icon}</div>
                      <div class="tool-title-section">
                        <h3 class="tool-title">{tool.name}</h3>
                        {tool.comingSoon && (
                          <span class="coming-soon-badge">即将推出</span>
                        )}
                      </div>
                    </div>

                    <div class="tool-description">
                      <p>{tool.description}</p>
                    </div>

                    <div class="tool-features">
                      <h4 class="features-title">主要功能</h4>
                      <ul class="features-list">
                        {tool.features.map((feature) => (
                          <li class="feature-item">{feature}</li>
                        ))}
                      </ul>
                    </div>

                    <div class="tool-footer">
                      {tool.comingSoon ? (
                        <button class="tool-link disabled" disabled>
                          <svg
                            width="16"
                            height="16"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="1.5"
                          >
                            <circle cx="12" cy="12" r="10" />
                            <polyline points="12,6 12,12 16,14" />
                          </svg>
                          敬请期待
                        </button>
                      ) : (
                        <a href={tool.url} class="tool-link">
                          <svg
                            width="16"
                            height="16"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="1.5"
                          >
                            <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
                            <polyline points="15,3 21,3 21,9" />
                            <line x1="10" y1="14" x2="21" y2="3" />
                          </svg>
                          立即使用
                        </a>
                      )}
                    </div>
                  </article>
                ))}
              </div>
            </div>
          ))
        }
      </section>

      <!-- 使用说明 -->
      <section class="usage-section">
        <div class="usage-content industrial-border">
          <h2 class="usage-title">使用说明</h2>
          <div class="usage-grid">
            <div class="usage-item">
              <div class="usage-icon">🚀</div>
              <h3>快速访问</h3>
              <p>
                所有工具都经过优化，无需注册即可直接使用，提供最佳的用户体验。
              </p>
            </div>
            <div class="usage-item">
              <div class="usage-icon">🔒</div>
              <h3>隐私保护</h3>
              <p>
                所有数据处理都在本地进行，不会上传到服务器，确保你的数据安全。
              </p>
            </div>
            <div class="usage-item">
              <div class="usage-icon">📱</div>
              <h3>响应式设计</h3>
              <p>完美适配各种设备，无论是桌面端还是移动端都能获得良好体验。</p>
            </div>
            <div class="usage-item">
              <div class="usage-icon">⚡</div>
              <h3>持续更新</h3>
              <p>定期添加新工具和功能，根据用户反馈不断改进和优化。</p>
            </div>
          </div>
        </div>
      </section>
    </div>
  </main>
</Layout>

<style>
  .main {
    min-height: calc(100vh - 80px);
    padding: 2rem 0;
  }

  .page-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .page-title {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  .page-description {
    font-size: 1.25rem;
    color: var(--color-secondary);
    max-width: 600px;
    margin: 0 auto 2rem;
    line-height: 1.6;
    font-family: var(--font-sans);
  }

  .tools-stats {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 1.5rem;
    padding: 1rem;
    background-color: var(--color-card);
    border: 2px solid var(--color-border);
    border-radius: 8px;
    max-width: 400px;
    margin: 0 auto;
  }

  .stat {
    text-align: center;
  }

  .stat-number {
    display: block;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-accent);
    font-family: var(--font-mono);
  }

  .stat-label {
    font-size: 0.75rem;
    color: var(--color-secondary);
    text-transform: uppercase;
    letter-spacing: 1px;
    font-family: var(--font-mono);
  }

  .stat-divider {
    color: var(--color-border);
    font-size: 1.2rem;
    font-weight: bold;
  }

  .tools-section {
    margin-bottom: 4rem;
  }

  .category-section {
    margin-bottom: 3rem;
  }

  .category-title {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--color-accent);
    border-bottom: 2px solid var(--color-border);
    padding-bottom: 0.5rem;
  }

  .tools-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 2rem;
  }

  .tool-card {
    background-color: var(--color-card);
    padding: 0;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    height: 100%;
    overflow: hidden;
  }

  .tool-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .tool-card.coming-soon {
    opacity: 0.7;
    background-color: var(--color-code-bg);
  }

  .tool-header {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.5rem 1.5rem 1rem;
  }

  .tool-icon {
    font-size: 2.5rem;
    flex-shrink: 0;
  }

  .tool-title-section {
    flex: 1;
  }

  .tool-title {
    font-family: var(--font-mono);
    font-size: 1.125rem;
    font-weight: 700;
    margin: 0 0 0.5rem 0;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--color-text);
    line-height: 1.2;
  }

  .coming-soon-badge {
    display: inline-block;
    background-color: var(--color-secondary);
    color: var(--color-bg);
    padding: 0.25rem 0.5rem;
    font-size: 0.7rem;
    font-family: var(--font-mono);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    border-radius: 12px;
  }

  .tool-description {
    padding: 0 1.5rem 1rem;
    flex-grow: 1;
  }

  .tool-description p {
    color: var(--color-secondary);
    font-size: 0.9rem;
    line-height: 1.5;
    margin: 0;
    font-family: var(--font-sans);
  }

  .tool-features {
    padding: 0 1.5rem 1rem;
  }

  .features-title {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--color-accent);
  }

  .features-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .feature-item {
    background-color: var(--color-code-bg);
    color: var(--color-text);
    padding: 0.25rem 0.5rem;
    border: 1px solid var(--color-border);
    font-size: 0.75rem;
    font-family: var(--font-mono);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    border-radius: 4px;
  }

  .tool-footer {
    padding: 1rem 1.5rem 1.5rem;
    border-top: 1px solid var(--color-border);
    margin-top: auto;
  }

  .tool-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    background-color: var(--color-accent);
    color: var(--color-bg);
    text-decoration: none;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    border: 2px solid var(--color-accent);
    border-radius: 4px;
    transition: all 0.2s ease;
    cursor: pointer;
  }

  .tool-link:hover {
    background-color: var(--color-text);
    border-color: var(--color-text);
    transform: translateY(-1px);
  }

  .tool-link.disabled {
    background-color: var(--color-muted);
    border-color: var(--color-muted);
    cursor: not-allowed;
    opacity: 0.6;
  }

  .tool-link.disabled:hover {
    transform: none;
  }

  .usage-section {
    margin-bottom: 2rem;
  }

  .usage-content {
    background-color: var(--color-card);
    padding: 2rem;
  }

  .usage-title {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 2rem;
    text-align: center;
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--color-accent);
  }

  .usage-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
  }

  .usage-item {
    text-align: center;
  }

  .usage-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .usage-item h3 {
    font-family: var(--font-mono);
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .usage-item p {
    color: var(--color-secondary);
    font-size: 0.9rem;
    line-height: 1.5;
    margin: 0;
    font-family: var(--font-sans);
  }

  /* 响应式设计 */
  @media (max-width: 768px) {
    .page-title {
      font-size: 2rem;
    }

    .page-description {
      font-size: 1rem;
    }

    .tools-stats {
      flex-direction: column;
      gap: 1rem;
    }

    .stat-divider {
      display: none;
    }

    .tools-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .tool-header {
      padding: 1rem 1rem 0.75rem;
    }

    .tool-description {
      padding: 0 1rem 0.75rem;
    }

    .tool-features {
      padding: 0 1rem 0.75rem;
    }

    .tool-footer {
      padding: 0.75rem 1rem 1rem;
    }

    .usage-content {
      padding: 1.5rem;
    }

    .usage-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }
</style>
